<Window x:Class="alesya_rassylka.SelectRecipientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:alesya_rassylka"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Выбор получателей" Height="500" Width="600" 
        Background="#DFE3EB" WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#172A74"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#172A74"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#172A74"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E6F8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C0D0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <local:CategoriesConverter x:Key="CategoriesConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Категория:" Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
            <ComboBox x:Name="CategoryComboBox" 
                      Width="300" Margin="10,0,0,0" Height="30"
                      ItemsSource="{Binding Categories}">
                <ComboBox.Template>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="White" BorderBrush="#172A74" BorderThickness="1">
                                <ToggleButton.Content>
                                    <TextBlock x:Name="SelectedCategoriesText" 
                                               Text="Выберите категории..." 
                                               Foreground="#9E9E9E"/>
                                </ToggleButton.Content>
                            </ToggleButton>
                            <Popup x:Name="Popup" 
                                   IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom" 
                                   AllowsTransparency="True" 
                                   PopupAnimation="Slide"
                                   StaysOpen="True">
                                <Border BorderBrush="#172A74" BorderThickness="1" Background="White" MaxHeight="200">
                                    <ListBox x:Name="CategoryListBox" 
                                             SelectionMode="Multiple"
                                             ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                             SelectionChanged="CategoryListBox_SelectionChanged"
                                             PreviewMouseRightButtonDown="CategoryListBox_PreviewMouseRightButtonDown">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Content="{Binding}" 
                                                          IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Mode=TwoWay}"
                                                          Margin="5"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Добавить категорию" Click="AddCategory_Click"/>
                                                <MenuItem Header="Удалить категорию" Click="DeleteCategory_Click" MouseDown="MenuItem_MouseDown"/>
                                                <MenuItem Header="Редактировать категорию" Click="EditCategory_Click" MouseDown="MenuItem_MouseDown"/>
                                            </ContextMenu>
                                        </ListBox.ContextMenu>
                                    </ListBox>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </ComboBox.Template>
            </ComboBox>
        </StackPanel>

        <TextBox x:Name="SearchTextBox" Height="30" Margin="0,0,0,10" Grid.Row="1"
                 VerticalContentAlignment="Center"
                 TextChanged="SearchTextBox_TextChanged">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Foreground" Value="#172A74"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Grid>
                                    <TextBlock x:Name="Placeholder" Text="Поиск..." 
                                               Foreground="#9E9E9E"
                                               VerticalAlignment="Center"
                                               Margin="5,0,0,0"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"/>
                                    <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
        </TextBox>

        <ListBox x:Name="RecipientsListBox" Grid.Row="2" SelectionMode="Multiple"
                 BorderBrush="#172A74" BorderThickness="1" Background="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Email}" FontSize="12" Foreground="Gray"/>
                        <TextBlock Text="{Binding Categories, Converter={StaticResource CategoriesConverter}}" 
                                   FontSize="12" Foreground="#3050A5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Добавить" Click="AddRecipient_Click"/>
                    <MenuItem Header="Удалить" Click="DeleteRecipient_Click"/>
                    <MenuItem Header="Редактировать" Click="EditRecipient_Click"/>
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0,10,0,0">
            <Button Content="Применить" Style="{StaticResource ActionButton}" Click="ConfirmSelection_Click" Width="100" Margin="0,0,10,0"/>
            <Button Content="Отменить" Style="{StaticResource ActionButton}" Click="Cancel_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>